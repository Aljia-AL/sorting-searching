ALGORITHM insertion_sort
VAR
    i,j,key : INTEGER;
    tab: ARRAY_OF INTEGER[]; [9,15,22,4,1]
BEGIN
 for (i:= 0 ; i<n , i++)
 {
    key := tab[i]
    j := i-1;
 }
 WHILE (j = 0 && arr[j] >key) DO
    {
        arr[j+1] = arr[j];
        j = j-1;
    }
    arr[j+1] = key;
 END_WHILE
END
